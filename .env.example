# 운영 배포 환경변수 예시 — 실제 비밀값은 GitHub Secrets 로 관리합니다.
# 이 파일은 변수명을 안내하기 위한 템플릿일 뿐, 운영 값은 비워두세요.

# 앱
PORT=8000
NODE_ENV=production

# 풀스택일 때만 — DB 접속 정보 (예시)
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname
# REDIS_URL=redis://host:6379/0
